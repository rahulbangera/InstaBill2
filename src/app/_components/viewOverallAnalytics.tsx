"use client";

import { useEffect, useState } from "react";

import { api } from "~/trpc/react";

const ViewOverallAnalytics = ({ shopId }: { shopId: string }) => {
    return (
        <div>
            <h2 className="text-center">Coming Soon</h2>
        </div>
    );
}

export default ViewOverallAnalytics;